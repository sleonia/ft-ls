#include "types.h"

void	reverse(t_file **files)
{
//	t_file *tmp;
//	t_file *prev;
//	t_file *next;
////
//	if (!files || !*files)
//		return ;
//	tmp = *files;
//	prev = NULL;
//	while (tmp)
//	{
//		next = tmp->next;
//		tmp->next = prev;
//		prev = tmp;
//		tmp = tmp->next;
//	}
}
